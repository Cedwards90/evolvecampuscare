/**
 * Deprecated: replaced by useRealtimeBridge. Kept as a no-op for backward
 * compatibility with any callers.
 */
export function useRealtimeStudentAssignments(_caseManagerId?: string | undefined) {
  // no-op
}
